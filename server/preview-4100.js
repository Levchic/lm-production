/* Запуск копии сайта на отдельном порту, чтобы старую и новую версию
   можно было держать открытыми рядом. Обычный запуск — start.command. */
process.env.PORT = process.env.PORT || "4100";
require("./server.js");
